# Handoff: <Task-ID> — <Short Title>

**Generated by:** Claude (architect)
**Date:** <date>
**GitHub Issue:** #<issue-number> — <issue title>
**Branch:** `<branch-name>`
**Implements:** `<WP-ID or feature description>` — see planning doc at `<path to WP or task doc>`

---

## Context

<!-- 2-3 sentences: why this task exists, what problem it solves, how it fits the current milestone -->

---

## Spec References

Read these documents before writing any code:

| Doc | Why |
|---|---|
| `spec/...` | <what constraint / interface it defines> |
| `design/.../...ModuleDesign.md` | <what design decision it records> |

---

## What to Build

### New Files

| File | Purpose |
|---|---|
| `src/openqilin/<module>/foo.py` | <one-line description> |
| `tests/unit/test_<task>_<name>.py` | Unit tests for the above |

### Modified Files

| File | What changes |
|---|---|
| `src/openqilin/<module>/bar.py` | <specific change — e.g., "add method X with signature Y"> |

---

## Interfaces and Signatures

<!-- Exact function/class signatures CodeX must implement. Do not deviate. -->

```python
class FooService:
    def do_thing(self, arg: SomeType) -> ResultType:
        """
        <contract description>
        Raises: SomeError on failure (fail-closed).
        """
        ...
```

---

## Behaviour Specification

<!-- Step-by-step description of what the code must do. Numbered list. -->

1. ...
2. ...
3. On error, raise `X` — do not return a default value.

---

## Tests to Write

| Test name | What it asserts |
|---|---|
| `test_<name>_happy_path` | <normal case> |
| `test_<name>_fail_closed` | error input → error raised, not swallowed |

All unit tests must pass `uv run pytest tests/unit/ -x`.

---

## Acceptance Criteria

Run this validation matrix. All must PASS before committing.

```bash
# Governance gate
grep -r --include="*.py" -l "class InMemory" . | grep -v "/testing/" | grep -v "tests/"
# ↑ must return no output

# Static checks
uv run ruff check .
uv run ruff format --check .
uv run mypy .

# Tests
uv run pytest tests/unit tests/component -x
```

---

## Out of Scope

<!-- Things CodeX must NOT do for this task. Be explicit. -->

- Do not modify `<other_module>` — that is a separate task.
- Do not add new Alembic migrations unless explicitly listed above.

---

## Spec Change Requests

<!-- Leave blank. CodeX fills this in if a spec conflict is discovered during implementation. -->
<!-- Format: describe the conflict, which docs are involved, and what the blocking question is. -->
<!-- The Architect will resolve and update the spec before the PR can be merged. -->
