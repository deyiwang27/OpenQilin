OpenQilin Implementation TODO Tracker
Date: 2026-03-13
Stage: implementation/v1
Archive status: active

Tracking model:
- GitHub Issues/PRs/Project are the execution source of truth.
- This file is a concise working checklist for active milestones.
- Detailed status/evidence authority is `implementation/v1/planning/ImplementationProgress-v1.md`.

Status legend:
- `todo`
- `in_progress`
- `blocked`
- `done`

Current context:
- Active branch: `feat/mvp-1-validation`
- Current scope: post-MVP Discord multi-bot role UI implementation execution (`M10`)
- Baseline roadmap: `implementation/v1/mvp/MvpArchitectureRoadmap-v1.md`

Milestone summary:
- `M0`..`M4`: `done` (see `implementation/v1/planning/ImplementationProgress-v1.md` and evidence packs)
- `M5`: `done`
- `M6`: `done`
- `M7`: `done`
- `M8`: `done`
- `M9`: `done`
- `M10`: `in_progress`

Checklist:
1) `M5 MVP Proposal and Governance Activation` (`issue #29`) - `done`
- `M5-WP1` project lifecycle guard lock - `done` (`issue #30`)
- `M5-WP2` proposal discussion + triad approval APIs (`owner`, `ceo`, `cwo`) - `done` (`issue #31`)
- `M5-WP3` CWO initialization flow (scope/objective/budget/metrics) - `done` (`issue #32`)
- `M5-WP4` workforce template binding (`project_manager` active, `domain_leader` declared-disabled) - `done` (`issue #33`)
- milestone closeout PR + parent issue close for `#29` - `done` (`PR #34`, merge `b997c46`)

2) `M6 MVP Documentation and Access Governance` (`issue #35`) - `done`
- kickoff/decomposition complete with child work packages (`#36`..`#39`) - `done`
- MVP policy decisions locked (doc-type enum, per-type + total caps, stage/role write matrix, hybrid mutability, pointer/hash integrity semantics) - `done`
- `M6-WP1` canonical project file root (`${OPENQILIN_SYSTEM_ROOT}/projects/<project_id>/`) + runtime pointer/hash model (DB target deferred) - `done` (`issue #36`)
- `M6-WP2` project document policy (approved types + per-type caps + fail-closed over-cap) - `done` (`issue #37`)
- `M6-WP3` specialist touchability enforcement (`project_manager`-only direct path) - `done` (`issue #38`)
- `M6-WP4` Project Manager mandatory-operations template enforcement - `done` (`issue #39`)
- M6 remediation batch (specialist spoof hardening, six baseline activation docs, role/stage write authorization) - `done`
- milestone closeout PR + parent issue close for `#35` - `done` (`PR #40`)

3) `M7 MVP Persistence, Adapter, and Acceptance` (`issue #41`) - `done`
- kickoff/decomposition complete with child work packages (`#42`..`#47`) - `done`
- `M7-WP1` persistent runtime-state adapters + startup recovery hardening - `done` (`issue #42`)
- `M7-WP2` Discord ingress context + identity/channel mapping baseline (`guild_id`/`channel_id`/`channel_type`; allowlist states `pending|verified|revoked`) - `done` (`issue #43`)
- `M7-WP3` Discord chat-governance enforcement (`direct|leadership_council|governance|executive|project`) + lifecycle membership resolver + specialist access constraints + pending-role profile flags (`secretary`, `cso`, `domain_leader`) - `done` (`issue #44`)
- `M7-WP4` Docker `full` profile runtime cutover (`api_app`/workers placeholder replacement) - `done` (`issue #45`)
- `M7-WP5` Gemini Flash free-tier provider-path activation + quota telemetry validation - `done` (`issue #46`)
- `M7-WP6` MVP acceptance matrix + evidence pack closeout - `done` (`issue #47`)
- M7 post-review remediation: governed project creation API, completion-governance chain enforcement, and Discord connector-ingress adapter path - `done`
- M7 closeout status synchronized in docs/issues (M9 tracked separately) - `done`

4) `M8 MVP Governance Surface Hardening` (`issue #48`) - `done`
- kickoff/decomposition complete with child work packages (`#50`..`#52`) - `done`
- `M8-WP1` connector-auth enforcement on governance/discussion routes - `done` (`issue #50`)
- `M8-WP2` governed lifecycle APIs for `pause|resume|terminate|archive` - `done` (`issue #51`)
- `M8-WP3` API-only acceptance lifecycle refactor + planning doc alignment - `done` (`issue #52`)

5) `M9 MVP Real Discord Runtime and Live Validation` (`issue #49`) - `done`
- kickoff/decomposition complete with child work packages (`#53`..`#56`) - `done`
- `M9-WP1` real Discord bot worker runtime (ingress + response bridge) - `done` (`issue #53`)
- `M9-WP2` Docker `full` profile integration + non-local secret hardening - `done` (`issue #54`)
- `M9-WP3` live Discord end-to-end MVP acceptance validation - `done` (`issue #55`, live Discord ingress verified with accepted command response, and both lifecycle acceptance branches executed under live runtime with deterministic evidence artifacts)
- `M9-WP4` live-instance evidence pack and closeout - `done` (`issue #56`, evidence pack finalized with preflight/manifest/notes plus runtime ps/log snapshots and governance execution artifact)
- post-closeout remediation for Discord role-fidelity and LLM role-lock hardening - `done` (`issue #60`, recipient role-id mismatch fail-closed guard + role-labeled Discord replies + role-locked prompt/injection denial + per-recipient conversation memory tests)
- issue `#60` follow-up resilience hardening - `done` (Gemini transient `429`/`5xx` retry-backoff path, compose env wiring for LLM token/retry settings, larger Discord response cap, and runtime restart verification)

6) `M10 Multi-Bot Discord Role UI` (`issue #61`) - `in_progress`
- kickoff/decomposition complete with child work packages (`#62`..`#67`, `#69`..`#71`) - `done`
- architecture design doc published (`implementation/v1/mvp/MultiBotDiscordArchitecture-v1.md`) - `done`
- `M10-WP1` role-bot identity registry + token/config hardening - `done` (`issue #62`)
- `M10-WP2` multi-bot worker runtime + event fan-in - `done` (`issue #63`)
- `M10-WP3` DM + mention group-chat recipient resolver + governed routing - `done` (`issue #64`)
- `M10-WP4` role-locked prompts + per-role memory isolation - `done` (`issue #65`)
- `M10-WP4` grounding follow-up: `llm_reason` grounded-only enforcement (DB/project-doc source of truth + citation fail-closed contract) - `done` (`issue #68`)
- `M10-WP5` outbound delivery hardening (chunking + sequencing) - `done` (`issue #66`)
- `M10-WP6` live acceptance evidence pack + operator runbook - `in_progress` (`issue #67`, automation/check scripts and artifact conformance delivered; file-based role-bot token registry config support added via `OPENQILIN_DISCORD_ROLE_BOT_TOKENS_FILE`; final live Discord evidence capture pending operator run)
- `M10-WP7` intent-level read tool surface for grounded answers - `done` (`issue #69`)
- `M10-WP8` governed write-action tools for project/runtime mutations - `done` (`issue #70`)
- `M10-WP9` tool orchestration policy + role access control + acceptance - `done` (`issue #71`)

Carryover follow-ups:
- enforce non-default connector shared secret in non-local environments at runtime validation layer - `todo`

Update routine:
- On issue start: add/update item here with issue id and status.
- On PR open/merge: update related item status and link PR in issue evidence.
- Weekly: sync milestone percentages into `implementation/v1/planning/ImplementationProgress-v1.md`.
